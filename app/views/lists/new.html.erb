<% provide(:title, 'Nueva lista') %>
<h1> Nueva lista de tareas </h1>
<div class="row">
  <div class="span6 offset3">
    <%= form_for(@list) do |f| %>
     <%#= render 'shared/error_messages' %>

      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.submit "Crear", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
