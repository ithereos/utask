<h1>Listas de tareas creadas</h1>

<ul>
	<% @lists.each do |list| %>
	<li>
		<%= link_to list.name, list_path(list) %> |
		<%= link_to "Eliminar", list, method: :delete,
                                  data: { confirm: "Confirmar la eliminación del cliente" } %>
        <%= link_to "Editar", edit_list_path(list) %>

	</li>
	<% end %>

	<%= link_to "Nueva lista", new_list_path, class: "btn btn-large btn-primary" %>
</ul>
