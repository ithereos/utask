<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "uTask: Gestor de tareas", lists_path, id: "logo" %>
      <nav>
        <ul class="nav pull-right">
         

          <% if signed_in? %>        
        
            <li><%= link_to "Inicio", lists_path %></li>            
            <% if current_user.admin? %>
              <li><%= link_to "Usuarios", users_path %></li>
            <% end %>
            
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Cuenta <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Perfil", current_user %></li>
                <li><%= link_to "Actualizar datos", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Salir", signout_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Ingresar", signin_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>